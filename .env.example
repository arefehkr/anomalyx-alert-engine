# Optional. Only needed if you want to use a different local Ollama model
# than the default ("llama3.2"). No API key required, no cost -- everything
# runs locally through Ollama.
OLLAMA_MODEL=llama3.2
